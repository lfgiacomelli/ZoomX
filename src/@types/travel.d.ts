export type TravelProps = {
    via_codigo: number;
    via_origem: string;
    via_destino: string;
    via_formapagamento: string;
    via_servico: string;
    via_data: string;
    via_observacoes: string;
    via_valor: string;
    via_status: string;
}